import type { CliNamespace } from "../types.js";

export const evalNamespace: CliNamespace = {
	name: "eval",
	summary: "Score generated outputs for quality, specificity, and readiness.",
	commands: [],
};
