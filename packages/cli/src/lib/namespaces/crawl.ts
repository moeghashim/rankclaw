import type { CliNamespace } from "../types.js";

export const crawlNamespace: CliNamespace = {
	name: "crawl",
	summary: "Crawl source URLs and materialize extraction-ready page captures.",
	commands: [],
};
